spring:
  application:
    name: ssu-lost

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true

  kakao:
    kakao_client_id: ${KAKAO_REST_API_KEY}
    kakao_redirect_uri: http://localhost:8080/callback/kakao

  google:
    google_client_id: ${GOOGLE_REST_API_KEY}
    google_redirect_uri: http://localhost:8080/callback/google
    google_client_secret: ${GOOGLE_CLIENT_SECRET}

  web:
    resources:
      add-mappings: false
